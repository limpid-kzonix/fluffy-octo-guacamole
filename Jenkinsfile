properties([
  buildDiscarder(
    logRotator(
      artifactDaysToKeepStr: '2', 
      artifactNumToKeepStr: '2', 
      daysToKeepStr: '1', 
      numToKeepStr: '1',
    ),
  ),
])
pipeline {
  agent any
  stages {
    stage('') {
      steps {
        echo 'Stub.'
      }
    }
  }
}
